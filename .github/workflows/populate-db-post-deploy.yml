name: populate-db-post-deploy.yml
on:
  deployment_status:
    types: [success]

jobs:
  call-setup:
    runs-on: ubuntu-latest
    steps:
      - env:
          DB_SETUP_URL: website-nameslist-api-backend.vercel.app/api/setup?secret={{secrets.DB_SETUP_SECRET}}
        run:
          curl -s -X GET "$DB_SETUP_URL"



